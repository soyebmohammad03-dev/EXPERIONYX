"""Advanced reproducibility framework (Phase 20): a classification layer over every engine's own
`replay_check`, distinguishing exact/deterministic/tolerance/statistical/provenance-only agreement
instead of one binary "reproducible" flag. Never promises bit-for-bit reproduction the platform
cannot guarantee (see docs/reproducibility.md)."""
