"""Evidence and failure knowledge graph (Phase 17-18): a queryable, provenance-aware graph over
every existing entity kind. Edges are evidence/provenance relationships derived from the registry's
own references; they are never an automatically-inferred causal claim (see docs/graph.md)."""
