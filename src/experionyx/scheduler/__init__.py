"""Experiment scheduler and orchestration (Phase 17): durable, resumable, dependency-aware
execution of collections of experiments across the existing laboratory engines. It plans and
dispatches; it never re-implements the science of any engine it calls."""
